# Concept glossary for `sb cortex link` (Phase 2 of graph-augmented-memory).
#
# `concepts` are kebab-case slugs (mirroring canonical-tags.yml). Each is
# linked at the first body mention as [[slug]] (or [[slug|Surface]] when the
# prose case differs). Short slugs (< actions.linking.min-word-length, default
# 5) only link via an `aliases` entry whose surface form is long enough.
#
# `aliases` map a surface form found in prose to the canonical slug; the linker
# emits a piped wikilink [[slug|surface]] preserving the displayed wording.
# Seeded from the AI/tech vocabulary already evidenced across the vault.

concepts:
  - graphrag
  - langchain
  - langgraph
  - llamaindex
  - pinecone
  - weaviate
  - chromadb
  - neo4j
  - cognee
  - mem0
  - memgraphrag
  - fastembed
  - tree-sitter
  - claude-code
  - anthropic
  - tinygrad
  - ollama
  - llama-cpp
  - huggingface
  - transformers
  - tokenizers
  - reranking
  - embeddings
  - fine-tuning
  - quantization
  - distillation
  - kubernetes
  - terraform
  - postgres
  - sqlite
  - duckdb
  - obsidian
  - syncthing
  # Promoted from entity-proposals.yml (2026-06-06): genuine tools/concepts the
  # corpus actually names, to densify the high-signal wikilink layer.
  - mcp
  - tmux
  - fzf
  - zoxide
  - yazi
  - ratatui
  - playwright
  - supabase
  - vercel
  - n8n
  - github-cli
  - homebrew
  - ffmpeg
  - redis
  - podman
  - proxmox
  - jellyfin
  - immich
  - pi-hole
  - home-assistant
  - raspberry-pi
  - portainer
  - uptime-kuma
  - plex
  - notebooklm
  - tdd
  - cuda
  - opencode
  - windsurf

aliases:
  Retrieval-Augmented Generation: rag
  retrieval augmented generation: rag
  Graph RAG: graphrag
  GraphRAG: graphrag
  Reciprocal Rank Fusion: rrf
  Model Context Protocol: mcp
  Large Language Model: llm
  large language models: llm
  Claude Code: claude-code
  LangChain: langchain
  LlamaIndex: llamaindex
  Hugging Face: huggingface
  tree-sitter: tree-sitter
  knowledge graph: knowledge-graph
  vector database: vector-database
  approximate nearest neighbor: ann
  # Multi-word surfaces for the 2026-06-06 promotions (short slugs like mcp/fzf/
  # tdd only auto-link via a surface form long enough to clear min-word-length).
  Home Assistant: home-assistant
  GitHub CLI: github-cli
  Uptime Kuma: uptime-kuma
  Raspberry Pi: raspberry-pi
  Test-Driven Development: tdd
  test-driven development: tdd
  NotebookLM: notebooklm
